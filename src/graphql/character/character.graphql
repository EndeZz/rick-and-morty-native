query getCharacterDetails($id: ID!) {
  character(id: $id) {
    id
    name
    status
    species
    type
    gender
    image
    origin {
      id
      name
    }
    location {
      id
      name
      type
    }
    episode {
      id
      name
      air_date
      episode
    }
  }
}
